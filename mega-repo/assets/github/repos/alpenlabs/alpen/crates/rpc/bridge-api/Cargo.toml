[package]
edition = "2021"
name = "strata-bridge-rpc-api"
version = "0.1.0"

[lints]
workspace = true

[dependencies]
strata-state.workspace = true

bitcoin.workspace = true
jsonrpsee = { workspace = true, features = ["server", "macros"] }

[features]
client = ["jsonrpsee/client"]
